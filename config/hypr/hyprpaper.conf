#
# hyprpaper — 壁紙
#
# path が実在しないと壁紙が出ないだけで、エラーメッセージも目立ちません。
# setup.sh が ImageMagick でグラデーション壁紙を生成してここに置きます。
# 好きな画像に差し替えてください。
#

splash = false
ipc    = true

wallpaper {
    monitor  =
    path     = ~/Pictures/wallpapers/wall.png
    fit_mode = cover
}
